// Root entry point. Required once the project has native folders (post-prebuild /
// bare workflow). Delegates to expo-router's entry so file-based routing boots the
// same on iOS, Android, and web.
import 'expo-router/entry';
